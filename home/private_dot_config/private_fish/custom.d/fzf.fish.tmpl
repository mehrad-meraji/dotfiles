# FZF Configuration
# =================
# Key bindings come from the fzf.fish plugin (fisher). Change them with fzf_configure_bindings.

set -gx FZF_DEFAULT_COMMAND 'fd --type f --hidden --follow --exclude .git'
